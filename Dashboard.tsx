import React from 'react';

// This is a placeholder to prevent build errors.
// The correct component is located at `components/Dashboard.tsx`.
const Placeholder: React.FC = () => <div style={{display: 'none'}} data-testid="placeholder-dashboard" />;

export default Placeholder;
