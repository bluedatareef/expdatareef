import React from 'react';

// This component is unused. This is a placeholder to prevent build errors.
export const AiAssistantModal: React.FC = () => <div style={{display: 'none'}} data-testid="placeholder-ai-assistant-modal" />;
